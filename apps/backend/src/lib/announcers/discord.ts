import { Announcer } from '.'
import { config } from '../../config'

// Announce through the Discord bot by sending the message through it via
// protobuf
export class DiscordAnnouncer extends Announcer {
  async announce(form) {}
}
