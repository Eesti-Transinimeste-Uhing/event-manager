project:
  name: 'tiptap'
  description: 'Headless tiptap package for BE and FE'

tasks:
  test:
    command: 'ava'
    inputs:
      - 'src/**/*'
      - 'package.json'

  build:
    command: 'tsc'
    options:
      cache: false
