project:
  name: 'i18n'
  description: 'Internationalisation libraries and shared code'

tasks:
  build:
    command: 'tsc'
    inputs:
      - 'src/**/*'
      - 'package.json'
    outputs:
      - 'dist'
